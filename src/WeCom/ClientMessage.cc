#include "ClientMessage.h"

namespace wcbot {
namespace wecom {

bool ClientMessage::ParseCommon(tinyxml2::XMLElement *Root) {
  bool Ret = false;
  do {
    // todo
    Ret = true;
  } while (false);
  return Ret;
}

}  // namespace wecom
}  // namespace wcbot
